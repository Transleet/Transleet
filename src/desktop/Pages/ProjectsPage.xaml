<Page
    x:Class="Transleet.Desktop.Pages.ProjectsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Transleet.Desktop.Pages"
    xmlns:models ="using:Transleet.Desktop.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ListView ItemsSource="{x:Bind ViewModel.Projects,Mode=OneWay}">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="models:Project">
                <TextBlock Text="{x:Bind Name,Mode=OneWay}"></TextBlock>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</Page>
